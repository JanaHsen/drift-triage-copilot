[project]
name = "drift-triage-copilot"
version = "0.1.0"
description = "Drift Triage Co-Pilot — self-healing ML platform with supervisor agent. Service-specific deps live in backend/pyproject.toml and platform/pyproject.toml."
readme = "README.md"
requires-python = ">=3.12"

# Repo root has no runtime dependencies. Each service owns its own:
#   backend/pyproject.toml  — agent service (LangGraph, Anthropic, OpenAI, ...)
#   platform/pyproject.toml — model serving + ML training (FastAPI, sklearn, ...)
dependencies = []
